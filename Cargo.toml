[workspace]
resolver = "2"  # Use the new dependency resolver (requires Rust 1.51+)

members = [
    "polynomials",
    "shamir_secret_sharing",
    "sumcheck_protocol",
    "gkr_protocol", "polynomial_commitment_scheme",
]
